using System;
using System.Web.Mvc;
using Common.Logging;

namespace APKO.Helpers.Filters
{
    //public class CustomErrorAttribute : ActionFilterAttribute
    //{
    //    private readonly ILog log = LogManager.GetLogger("MVCEngine");

    //    public override void OnActionExecuted(ActionExecutedContext filterContext)
    //    {
    //        if (filterContext.Exception != null)
    //        {
    //            log.Error(filterContext.Exception.Message);
    //            throw new Exception(filterContext.Exception.Message);
    //        }
    //    }
    //}
}